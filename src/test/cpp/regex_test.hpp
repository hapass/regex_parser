#pragma once

#include <string.h>
#include "gtest/gtest.h"
#include "regex.hpp"

using namespace regex_parser;

// TEST(Regex, regex_matches_substring_once) {
//     Regex regex("match");
//     EXPECT_EQ(true, regex->match("ch"));
// }